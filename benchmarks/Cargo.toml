[package]
name = "benchmarks"
version = "0.1.0"
edition = "2024"

[dependencies]
art = { path = "../art" }

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "main"
harness = false
